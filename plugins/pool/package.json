{
    "name": "@trex/pool",
    "version": "0.1.0",
    "description": "Shared DuckDB connection pool with serialized write queue",
    "scripts": {
      "prepack": "make configure && make release && mv build/release/extension/*/*.trex . && ../extension-ci-tools/extension_signing/sign-if-key.sh"
    },
    "author": "",
    "license": "Apache-2.0",
    "trex": {
        "core": {
            "name": "pool"
        }
    }
}
