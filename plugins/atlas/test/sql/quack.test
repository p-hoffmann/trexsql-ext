# name: test/sql/circe_intro.test
# description: test circe extension basic functions
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT trex_atlas_hello('Sam');
----
Catalog Error: Scalar Function with name trex_atlas_hello does not exist!

# Require statement will ensure this test is run with this extension loaded
require circe

# Confirm the extension works
query I
SELECT trex_atlas_hello('Sam');
----
Circe Sam

query I
SELECT trex_atlas_openssl_version('Michael') ILIKE 'Circe Michael, my linked OpenSSL version is OpenSSL%';
----
true
