# name: test/sql/tpm.test
# description: test tpm extension
# group: [tpm]

# Before we load the extension, this will fail
statement error
SELECT tpm('Sam');
----
Catalog Error: Scalar Function with name tpm does not exist!

# Require statement will ensure the extension is loaded from now on
require tpm

require icu

# Confirm the extension works
query I
SELECT * from tpm('Sam');
----
TPM Sam 📦