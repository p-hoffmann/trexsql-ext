# name: test/sql/llama.test
# description: test llama extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT llama('Sam');
----
Catalog Error: Scalar Function with name llama does not exist!

# Require statement will ensure this test is run with this extension loaded
require llama

# Confirm the extension works
query I
SELECT llama('Sam');
----
Llama Sam 🦙

query I
SELECT llama_openssl_version('Michael') ILIKE 'Llama Michael, my linked OpenSSL version is OpenSSL%';
----
true
